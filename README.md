# Pricecharter Singles Uploader

## Setup
1. Install Node.js: https://nodejs.org
2. Clone this repo: `git clone https://github.com/YOUR_USERNAME/pokemon-singles-uploader.git`
3. Install dependencies: `npm install`
4. Create a `.env` file with your Shopify credentials:
