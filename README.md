## Silvercharter CLI

This repo hosts the in-store CLI for managing Shopify products via PriceCharting data.

### Getting Started

```
npm install
npm start
```

The CLI will prompt for Imports / Products / Settings. Environment variables live in `data/.env`.